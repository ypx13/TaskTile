<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TaskTile.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskTile"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.Resources>
            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
            
            <Style TargetType="MenuFlyoutPresenter" x:Key="TrayMenuFlyoutStyle">
                <Setter Property="Background" Value="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource SurfaceStrokeColorFlyoutBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="0,4" />
            </Style>
        </Grid.Resources>
        <!-- Native SystemTrayManager used instead of H.NotifyIcon -->

        <Grid x:Name="AppTitleBar"
              Height="48"
              VerticalAlignment="Top" />

        <Image x:Name="FrostedOverlay"
               Source="ms-appx:///Assets/scratches.png"
               Stretch="UniformToFill"
               Opacity="0.4"
               Visibility="Collapsed" />

        <NavigationView x:Name="NavView"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        SelectionChanged="NavView_SelectionChanged">
            
            <NavigationView.PaneHeader>
                <StackPanel Orientation="Horizontal" Spacing="8"
                            Padding="12,0,12,0"
                            Height="40"
                            VerticalAlignment="Center">
                    <TextBlock Text="TaskTile"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    <Border Background="{StaticResource TaskTileCyanBrush}"
                            CornerRadius="4"
                            Padding="6,2"
                            VerticalAlignment="Center">
                        <TextBlock Text="BETA"
                                   FontSize="10"
                                   FontWeight="Normal"
                                   Foreground="Black"
                                   VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </NavigationView.PaneHeader>
            
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="HomeItem" Tag="Home" Content="Home" PointerEntered="Item_PointerEntered" PointerExited="Item_PointerExited" PointerPressed="Item_PointerPressed">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" FontFamily="Segoe Fluent Icons" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="GroupsItem" Tag="Groups" Content="Groups" PointerEntered="Item_PointerEntered" PointerExited="Item_PointerExited" PointerPressed="Item_PointerPressed">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF168;" FontFamily="Segoe Fluent Icons" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
